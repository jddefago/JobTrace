# The JobTrace app itself needs NOTHING here — the server, database, and the
# assistant-driven Gmail sync all use only the Python standard library.
#
# Install these only if you turn on the "Scheduled with my keys" sync option
# (dashboard -> Gmail pill -> Settings). See SETUP.md.

# Required for the keys-based sync (classification via your Anthropic API key):
anthropic>=0.40.0

# Only needed if you pick the "Google Cloud OAuth" transport instead of the
# default Gmail app password (IMAP, which uses the standard library):
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
